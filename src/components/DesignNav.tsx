/**
 * Variant switcher pill. Hidden for now — Desk is the only featured
 * practice variant. The other variant routes still exist so they can
 * be revived later; restore the nav body when more than one variant
 * is shipping.
 */
export function DesignNav() {
  return null;
}
